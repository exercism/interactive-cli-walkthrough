::Linux architecture
# Determine process architecture on Linux

To determine your process architecture, paste
in the following command into your terminal:

```
uname -m
```

Hit `Enter` after you have pasted in the command and examine the output.

## `x86_64`

If the output is `x86_64` then you are on a 64-bit architecture.

The file you are looking for is `BINARY_NAME-linux-64bit.tgz`.

## `i686`

If the output is `i686` then you are on a 32-bit architecture.

The file you are looking for is `BINARY_NAME-linux-32bit.tgz`.

## `armv5l`, `armv6l`, `armv7l` or `armv8l`

If the output is `armv5l`, `armv6l`, `armv7l`
or `armv8l` then you are on an ARM architecture.

The file you are looking for is `BINARY_NAME-linux-arm-v5.tgz`,
`BINARY_NAME-linux-arm-v6.tgz`, `BINARY_NAME-linux-arm-v7.tgz`
or `BINARY_NAME-linux-arm-v8.tgz` (respectively).

## Other

If your output does not match any of the above, try talk to a volunteer.

---

## Do you know your system’s process architecture?

- [[Yes->Linux tarball]]
- [[No, I would like talk to a volunteer->Talk to a Volunteer]]
